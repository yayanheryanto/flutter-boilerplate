<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- File hasil kamera (cache internal) -->
    <cache-path
        name="camera_images"
        path="." />

    <!-- File dari external cache (beberapa device) -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- File dari external storage (galeri) -->
    <external-path
        name="external_files"
        path="." />

    <!-- File internal app -->
    <files-path
        name="internal_files"
        path="." />
</paths>